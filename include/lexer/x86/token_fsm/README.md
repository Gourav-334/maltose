# README


## 1. Description

This directory contains all the header files that are assembled on top on central FSM handler.


## 2. Directory Structure

- `token_fsm0.h` - Header to first part of token FSM that handles state `0` to `9`.
- `token_fsm1.h` - Header to second part of token FSM that handles state `10` to `19`.

---