/* Including guard to avoid multiple inclusions. */

#ifndef TOKEN_FSM0_H
	#define TOKEN_FSM0_H










/* Declaring functions. */

void token_fsm0(char *str, unsigned short int start, signed short int *state);		// Handles state 0-9 of token FSM.










/* Closing the guard macro. */

#endif