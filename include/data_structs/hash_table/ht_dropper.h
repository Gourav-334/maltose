/* Including guards to avoid multiple inclusions. */

#ifndef HT_DROPPER_H
	#define HT_DROPPER_H










/* Defining functions. */

void drop_ht(Ht_recorder *recorder, char *mode);		// Drops whole hash table, but retaining blueprint.










/* Closing guard macros. */

#endif