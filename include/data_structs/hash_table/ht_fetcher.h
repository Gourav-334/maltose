/* Including guards to avoid multiple inclusions. */

#ifndef HT_FETCHER_H
	#define HT_FETCHER_H





/* Including required headers. */

#include "ht_struct.h"










/* Declaring functions. */

void *fetch_ht_entry(Ht_recorder *recorder, void *key, char *mode);			// Fetches an entry from hash table.










/* Closing guard macros. */

#endif