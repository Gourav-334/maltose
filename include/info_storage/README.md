# README (`include/info_storage/`)


## 1. Description

This directory stores headers for source files that control virtual hardware state information like CPU registers, stacks, floating-point units, etc. Later these same values are mapped to binaries if no error is found.


## 2. Directory Structure

- `x86_info/` - Stores headers to source files for virtual hardware state information specific to **x86** processors.

---
