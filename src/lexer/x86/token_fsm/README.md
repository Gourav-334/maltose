# README


## 1. Description

This directory contains all the source codes that are assembled on top on central FSM handler.


## 2. Directory Structure

- `token_fsm0.c` - Source code of first part of token FSM that handles state `0` to `9`.
- `token_fsm1.c` - Source code of second part of token FSM that handles state `10` to `19`.

---